#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
  * main - Entry point
  *
  * Return: Always 0 (Success0
  */

int main(void)
{
	char Low;

	for (Low = 'a'; Low <= 'z'; Low++)
		putchar(Low);
		putchar('\n');

	return (0);

}
